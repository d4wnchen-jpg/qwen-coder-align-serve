# 求职任务交接文档（新对话必读）

> 用途：在新的对话里直接说「读 /Users/suchenyang/cs336/qwen-coder-align-serve/docs/交接文档.md 并继续」，即可无缝接上全部上下文。
> 前置对话完成的事：Dota2 解说视频项目（已完结，与本任务无关）；两个求职任务已完成调研、定位与项目骨架搭建。

## 目标

LLM 求职准备，两个任务（并行执行，总周期约 6 周）：

1. **给 vLLM 提交并合并一个 PR**（0 算力，本机 Mac 即可）
2. **DeepSeek/Qwen 架构下的代码领域 LLM 对齐 + 高吞吐部署系统**（租卡预算 ¥300 内）

## 已定决策

| 事项 | 决定 |
|---|---|
| 算力 | 可少量租卡，预算 ¥300（AutoDL/Vast.ai 4090 按时租为主） |
| 领域 | 代码/工程助手 |
| 基座模型 | 主力 Qwen3-Coder-30B-A3B-Instruct（4090 可 QLoRA 4bit）；备选 Qwen3-8B |
| 对齐技术栈 | LLaMA-Factory：QLoRA SFT（实验矩阵 r16/64 × e2/3）+ 可选 DPO |
| 数据 | Magicoder-OSS-Instruct-75K，筛 2-4k 子集，已带防污染清洗 |
| 评估 | EvalPlus（HumanEval+/MBPP+，pass@1） |
| 量化 | AWQ INT4 / FP8 |
| 部署 | vLLM：prefix caching + MTP 投机解码 + multi-LoRA 热切换 |
| vLLM PR 主攻 | [issue #33267](https://github.com/vllm-project/vllm/issues/33267)（unified_kv_cache_update 清理，KV-cache 核心路径） |
| vLLM PR 备胎 | [#28407 DCP 错误信息改进](https://github.com/vllm-project/vllm/issues/28407) |
| 简历叙事 | 两任务联动：上层对齐 + 底层推理系统 + vLLM 上游 contributor |

## 项目位置

```
/Users/suchenyang/cs336/qwen-coder-align-serve/    # 对齐+部署项目（骨架已建好，脚本语法已验证）
├── README.md                  # 项目说明 + 简历条目模板 + 5条命令复现
├── docs/执行计划.md            # 6周执行表 + ¥300预算拆分 + 风险表 + 验收标准（★必读）
├── data/prepare_data.py       # 数据清洗（本机可跑，¥0）
├── train/sft.yaml / dpo.yaml / run_sft.sh
├── eval/gen_solutions.py / run_evalplus.sh
├── serve/serve_base.sh / serve_quantized.sh / serve_lora.sh / benchmark.sh
├── scripts/benchmark_report.py
└── requirements.txt
```

## 当前进度与下一步

- [x] 调研完成（2026-08 实时数据）：vLLM good-first-issue 候选、multi-LoRA 官方博客、技术栈定稿
- [x] 项目骨架已创建，全部脚本通过语法检查
- [ ] 今天（¥0）：`git init` + 首次提交；跑 `data/prepare_data.py`（需先装 datasets）
- [ ] 今天：去 vLLM #33267 评论认领（"I'd like to work on this"），clone vLLM 到本地
- [ ] 按 docs/执行计划.md 的 W2-W6 推进（租卡时按预算表执行）

## 关键参考链接

- vLLM 贡献指南: https://docs.vllm.ai/en/v0.19.0/contributing/
- vLLM multi-LoRA 官方博客(2026-02): https://vllm.ai/blog/2026-02-26-multi-lora
- 微调+推理基础设施综述: https://zylos.ai/research/2026-03-22-open-source-llm-fine-tuning-serving-ai-agent-platforms/
- vLLM good-first-issue 列表: https://github.com/vllm-project/vllm/issues?q=label%3A%22good+first+issue%22

## 本机环境注意事项（新对话的 agent 会遇到）

- **机器**：macOS ARM（Apple Silicon），但系统 Python 3.9 是 x86_64（Rosetta）
- **系统 Python 有坑**：SSL 证书验证失败（urllib 不可用，用 curl 代替）；pip 装不了全局包（目录不可写）
- **可用 venv**：`/Users/suchenyang/cs336/dota_video/.venv`（内有 matplotlib / imageio-ffmpeg / Pillow / edge-tts），临时脚本可用它
- **npm 装包**：需 `HOME=<工作区内目录>` 重定向（家目录被沙箱禁写）
- **沙箱**：默认只能写工作区 `/Users/suchenyang/cs336`；写家目录（~/.dsh 等）会报 `[sandbox: file access denied]`，需用 sandbox_permissions 重试并说明理由
- **中文 TTS/图表**：中文字体用 `/System/Library/Fonts/Hiragino Sans GB.ttc`

## 新对话开场白模板

> 读 /Users/suchenyang/cs336/qwen-coder-align-serve/docs/交接文档.md，我们继续两个求职任务：vLLM PR 和代码领域 LLM 对齐+高吞吐部署。今天先把数据清洗跑起来并帮我起草 vLLM issue 的认领评论。
