# 待办清单（按执行顺序）

> 用法：每完成一项把 `⬜` 改成 `✅`。当前定位 → 阶段 1 的 serve/ 阅读。

## 阶段 1：读完剩余脚本（本机 ¥0）

| # | 文件 | 状态 | 重点 |
|---|---|---|---|
| 1 | `serve/serve_base.sh` | ✅ 已读 + A方案已做 | 还差：改模型路径 |
| 2 | `serve/serve_quantized.sh` | ⬜ | AWQ/FP8 两条路径 |
| 3 | `serve/serve_lora.sh` | ⬜ | multi-LoRA 挂载方式（招牌） |
| 4 | `serve/benchmark.sh` | ⬜ | RPS 阶梯、TTFT/TPOT |
| 5 | `scripts/benchmark_report.py` | ⬜ | 压测 JSON → 图表 |

## 阶段 2：改 serve 模型路径（读完就改）

- [ ] `serve_base.sh` 第 6 行 → `Qwen/Qwen3-8B-Instruct`；第 7 行 `--served-model-name` → `qwen3-8b`；第 2-3 行注释改对
- [ ] `serve_lora.sh` 第 17 行 → 换 8B；`served-model-name` 同步
- [ ] `serve_quantized.sh` AWQ 分支 → `Qwen/Qwen3-8B-AWQ`（官方）；bf16 分支 → `Qwen/Qwen3-8B-Instruct`
- [ ] 一致性：`--served-model-name` 与 `eval/run_evalplus.sh` 的 `--model` 对上（否则 404）

## 阶段 3：提交

```bash
bash -n serve/*.sh scripts/*.py
git add -A && git commit -m "serve/eval: 基座切 Qwen3-8B + 基线关 prefix caching"
```

## 阶段 4：上卡前终检（本机 ¥0）

- [ ] `bash -n` 所有 .sh；`py_compile` 所有 .py；`yaml.safe_load` 所有 .yaml
- [ ] 通读 README「快速开始」5 条命令，每条对应的脚本都能对上
- [ ] AutoDL 账号注册 + 充值（¥50 起）

## 阶段 5：租卡日 = W2 基线（4090 × 4-6h ≈ ¥12）

1. `nvidia-smi` 确认显卡
2. 装依赖（vllm 官方预编译版 + evalplus）
3. `export HF_ENDPOINT=https://hf-mirror.com`
4. 下载 `Qwen/Qwen3-8B-Instruct`（~16GB）
5. `bash serve/serve_base.sh` 起服务
6. `bash serve/benchmark.sh` 吞吐基线
7. `bash eval/run_evalplus.sh qwen3-8b baseline` EvalPlus 基线
8. 数字写进 `results/baseline.json`（含版本号/种子）

## 阶段 6：W3-W6（待展开）

- W3 训练矩阵（4 组）→ W4 量化+复测 → W5 压测 → W6 报告包装
